# ===========================================
# ZENNARA V2 BACKEND ENVIRONMENT VARIABLES
# ===========================================
# 
# ⚠️ SECURITY WARNING:
# - NEVER commit the actual .env file to version control
# - Use strong, unique passwords for all services
# - Change all default values before deploying to production
# - Rotate secrets regularly
# ===========================================

# ===========================================
# SERVER CONFIGURATION
# ===========================================
NODE_ENV=production
PORT=5000

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# ⚠️ IMPORTANT: Use a strong password and enable IP whitelisting
# MongoDB connection string format:
# mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://username:CHANGE_THIS_PASSWORD@cluster.mongodb.net/zennara?retryWrites=true&w=majority

# ===========================================
# JWT CONFIGURATION
# ===========================================
# ⚠️ CRITICAL: Generate a secure random string for JWT_SECRET
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=GENERATE_SECURE_RANDOM_STRING_HERE_MINIMUM_64_CHARACTERS
JWT_EXPIRE=7d

# ===========================================
# AWS CONFIGURATION
# ===========================================
# ⚠️ Use IAM roles with minimal permissions
AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID=YOUR_AWS_ACCESS_KEY
AWS_SECRET_ACCESS_KEY=YOUR_AWS_SECRET_KEY
AWS_S3_BUCKET_NAME=YOUR_S3_BUCKET_NAME

# AWS SES Email Configuration
AWS_SES_EMAIL=noreply@yourdomain.com
AWS_SES_REGION=ap-south-1

# ===========================================
# RAZORPAY PAYMENT GATEWAY
# ===========================================
# ⚠️ Use test keys for development, live keys for production only
RAZORPAY_KEY_ID=YOUR_RAZORPAY_KEY_ID
RAZORPAY_KEY_SECRET=YOUR_RAZORPAY_KEY_SECRET

# ===========================================
# TWILIO (WhatsApp) CONFIGURATION
# ===========================================
TWILIO_ACCOUNT_SID=YOUR_TWILIO_ACCOUNT_SID
TWILIO_AUTH_TOKEN=YOUR_TWILIO_AUTH_TOKEN
TWILIO_WHATSAPP_NUMBER=whatsapp:+1234567890

# ===========================================
# FIREBASE ADMIN SDK
# ===========================================
# ⚠️ Path to Firebase service account JSON file
FIREBASE_SERVICE_ACCOUNT_PATH=./config/firebase-service-account.json

# ===========================================
# CORS CONFIGURATION
# ===========================================
# ⚠️ Set allowed origins carefully
# Production admin panel URL
ADMIN_PANEL_URL=https://admin.yourdomain.com
# Development URLs (remove in production)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8081

# ===========================================
# SECURITY CONFIGURATION
# ===========================================
# Rate limiting (requests per window)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Session configuration
SESSION_TIMEOUT=7d
MAX_SESSIONS_PER_USER=5

# ===========================================
# OPTIONAL: IP WHITELIST FOR ADMIN
# ===========================================
# Comma-separated list of allowed IPs for admin panel
# ADMIN_IP_WHITELIST=203.0.113.0,198.51.100.0

# ===========================================
# LOGGING & MONITORING
# ===========================================
LOG_LEVEL=info
ENABLE_SECURITY_LOGGING=true
